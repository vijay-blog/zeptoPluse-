package com.nexamart.backend.repository;import com.nexamart.backend.domain.Earning;import org.springframework.data.domain.*;import org.springframework.data.jpa.repository.*;import java.time.Instant;import java.util.*;public interface EarningRepository extends JpaRepository<Earning,Long>{Page<Earning> findByPartnerId(Long id,Pageable p);List<Earning> findByPartnerIdAndEarnedAtBetween(Long id,Instant a,Instant b);}

# iOS runner

This directory is reserved for the Flutter iOS runner generated on macOS with
`flutter create --platforms=ios .`. The Flutter customer code is platform
neutral; Android is the active development target for this phase.

package com.daily.nexamartpartner.features.auth.data.model

// Registration returns the same token/session shape as login so a newly created
// delivery-partner account can enter the app immediately.
typealias RegistrationResponseDto = LoginResponseDto
